<div class="container">
  <%= get_flash(@conn, :error) %>
  
  <%= form_for @conn, auth_path(@conn, :login), [name: :login], fn f -> %>
    <%= password_input f, :password %>
    <%= submit "login" %>
  <% end %>
</div>
